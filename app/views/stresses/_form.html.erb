<%= form_with(model: stress, local: true) do |form| %>
  <% if stress.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(stress.errors.count, "error") %> prohibited this stress from being saved:</h2>

      <ul>
        <% stress.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.datetime_select :date %>
  </div>

  <div class="field">
    <%= form.label :stress %>
    <%= form.number_field :stress %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
